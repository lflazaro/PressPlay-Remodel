<Grid x:Class="PressPlay.Timeline.TimelineControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
      xmlns:local="clr-namespace:PressPlay.Timeline"
      xmlns:converters="clr-namespace:PressPlay.Converters"
      mc:Ignorable="d"
      Background="#2a2e32" MinHeight="150"
      PreviewMouseMove="TimelineControl_PreviewMouseMove"
      PreviewMouseLeftButtonUp="TimelineControl_PreviewMouseLeftButtonUp"
      PreviewMouseRightButtonDown="TracksCanvas_PreviewMouseRightButtonDown"
      PreviewDragOver="Timeline_PreviewDragOver"
      PreviewDrop="Timeline_PreviewDrop"
      PreviewKeyUp="Timeline_PreviewKeyUp"
      AllowDrop="True"
      Focusable="True"
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Tool Buttons -->
    <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1">
        <StackPanel Orientation="Horizontal">
            <Grid Width="100" />
            <!-- Selection Tool -->
            <RadioButton Style="{StaticResource {x:Type ToggleButton}}" Margin="1"
                         IsChecked="{Binding Project.SelectedTool, Converter={converters:TimelineSelectedToolToSelectedConverter SelectedTool=SelectionTool}}"
                         Command="{Binding ToolSelectedCommand, RelativeSource={RelativeSource AncestorType=local:TimelineControl}}"
                         CommandParameter="{x:Static local:TimelineSelectedTool.SelectionTool}"
                         ToolTip="Selection Tool">
                <svgc:SvgIcon UriSource="/Resources/Icons/select.svg" Fill="LightGray" Width="14" Height="14" Margin="4" />
            </RadioButton>
            <!-- Cutting Tool -->
            <RadioButton Style="{StaticResource {x:Type ToggleButton}}" Margin="1"
                         IsChecked="{Binding Project.SelectedTool, Converter={converters:TimelineSelectedToolToSelectedConverter SelectedTool=CuttingTool}}"
                         Command="{Binding ToolSelectedCommand, RelativeSource={RelativeSource AncestorType=local:TimelineControl}}"
                         CommandParameter="{x:Static local:TimelineSelectedTool.CuttingTool}"
                         ToolTip="Cutting Tool">
                <svgc:SvgIcon UriSource="/Resources/Icons/cut.svg" Fill="LightGray" Width="14" Height="14" Margin="4" />
            </RadioButton>
        </StackPanel>
    </Border>

    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="15" />
        </Grid.RowDefinitions>

        <!-- Track Headers -->
        <Grid Grid.Column="0" Width="{Binding Project.TrackHeadersWidth, RelativeSource={RelativeSource AncestorType=local:TimelineControl}}">
            <StackPanel>
                <Grid Height="30">
                    <TextBlock Text="Tracks" Foreground="WhiteSmoke" VerticalAlignment="Center" HorizontalAlignment="Center" />
                </Grid>
                <ItemsControl ItemsSource="{Binding Project.Tracks, RelativeSource={RelativeSource AncestorType=local:TimelineControl}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Height="{Binding Height}" Background="#232629" BorderBrush="Gray" BorderThickness="0.5">
                                <TextBlock Text="{Binding Name}" Margin="3,0" VerticalAlignment="Center" Foreground="WhiteSmoke"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>

        <!-- Timeline Canvas -->
        <ScrollViewer x:Name="tracksHScrollView" Grid.Column="1" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" PreviewMouseWheel="TracksScrollView_PreviewMouseWheel">
            <Canvas Width="2000" HorizontalAlignment="Left" PreviewMouseLeftButtonDown="TracksCanvas_PreviewMouseLeftButtonDown">
                <local:TimelineHeaderControl x:Name="header" Project="{Binding Project, RelativeSource={RelativeSource AncestorType=local:TimelineControl}}" />

                <ItemsControl x:Name="tracksControl" ItemsSource="{Binding Project.Tracks, RelativeSource={RelativeSource AncestorType=local:TimelineControl}}" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Margin="0,30,0,0">
                    <ItemsControl.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Paste" Click="PasteItem_Click" InputGestureText="Ctrl+V" />
                        </ContextMenu>
                    </ItemsControl.ContextMenu>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Height="{Binding Height}" Width="{Binding Width, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Background="#232629" BorderBrush="Gray" BorderThickness="0.5">
                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <local:TrackItemControl />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <Canvas />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="Canvas.Left">
                                                <Setter.Value>
                                                    <MultiBinding Converter="{converters:TrackDataItemToPositionConverter}">
                                                        <Binding Path="Project" RelativeSource="{RelativeSource AncestorType=local:TimelineControl}" />
                                                        <Binding Path="Project.TimelineZoom" RelativeSource="{RelativeSource AncestorType=local:TimelineControl}" />
                                                        <Binding Path="." />
                                                        <Binding Path="Position" />
                                                        <Binding Path="Start" />
                                                        <Binding Path="End" />
                                                    </MultiBinding>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                </ItemsControl>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <local:TimelineNeedleControl x:Name="needle">
                    <local:TimelineNeedleControl.Style>
                        <Style TargetType="local:TimelineNeedleControl">
                            <Setter Property="Canvas.Left">
                                <Setter.Value>
                                    <MultiBinding Converter="{converters:NeedlePositionConverter}">
                                        <Binding Path="Project" RelativeSource="{RelativeSource AncestorType=local:TimelineControl}" />
                                        <Binding Path="Project.TimelineZoom" RelativeSource="{RelativeSource AncestorType=local:TimelineControl}" />
                                        <Binding Path="Project.NeedlePositionTime" RelativeSource="{RelativeSource AncestorType=local:TimelineControl}" />
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </local:TimelineNeedleControl.Style>
                </local:TimelineNeedleControl>
            </Canvas>
        </ScrollViewer>

        <local:BindableScrollBar Grid.Column="1" Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom"
                                  BoundScrollViewer="{Binding ElementName=tracksHScrollView}" />
    </Grid>
</Grid>
